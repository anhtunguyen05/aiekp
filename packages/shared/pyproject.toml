[project]
name = "aiekp-shared"
version = "0.1.0"
description = "Core domain models and interfaces for AIEKP"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
